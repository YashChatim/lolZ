<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="LolZ.ChampionMasteryPage">
    <ContentPage.Content>
        <ListView x:Name="championMasteryListView">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <StackLayout Orientation="Horizontal">
                            <Label x:Name="championIdLabel" Text="{Binding ChampionId}" />
                            <Label x:Name="championLevelLabel" Text="{Binding ChampionLevel}" />
                            <Label x:Name="championPointsLabel" Text="{Binding ChampionPoints}" />
                            <Label x:Name="chestGrantedLabel" Text="{Binding ChestGranted}" />
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </ContentPage.Content>
</ContentPage>